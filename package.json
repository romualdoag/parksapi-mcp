{
  "name": "parksapi-mcp",
  "version": "0.1.0",
  "description": "Generic MCP server exposing everything @themeparks/parksapi supports (live wait times, entities, schedules)",
  "type": "module",
  "license": "MIT",
  "engines": {
    "node": ">=24"
  },
  "bin": {
    "parksapi-mcp": "dist/index.js"
  },
  "scripts": {
    "setup:upstream": "npm --prefix upstream install --no-audit --no-fund && npm --prefix upstream run build",
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsx src/index.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.17.0",
    "@themeparks/parksapi": "file:./upstream",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.20.5",
    "typescript": "^5.9.0"
  }
}
